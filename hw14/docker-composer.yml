services:
  web:
    build: . 
    networks:
      - my

  nginx:
    image: nginx
    ports:
      - "80:80"  
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro  # :ro означает read-only (только для чтения)
    networks:
      - my

networks:
  my: